.DS_Store
xcuserdata/
*.xcuserstate
DerivedData/
.build/
build/
.swiftpm/

# IDE state
.vscode/
.idea/

# Local-only progress tracking, not for upstream
TODO.md

# Unit tests stay local — they document license/trial internals
TuttiTests/

# Signing & credentials must never be committed
*.certSigningRequest
*.mobileprovision
*.p12
*.cer
*.pem
*.key

# Env files with potential secrets
.env
.env.*

# Built artifacts that may land in the repo root
*.dmg
*.zip
*.app

# Sparkle release tooling (downloaded, not source-controlled)
.sparkle-tools/

# Local-only xcodegen overlay for TuttiTests target (TuttiTests/ is gitignored)
project.tests.yml
